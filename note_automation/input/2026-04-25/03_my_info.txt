# 一次情報

## 構成案（確定）

タイトル：basic-memory × Google Driveがあらためて有能だった。2環境・複数リポの記憶を横断する話

ターゲット：複数PC・複数リポジトリを使っていて、Claudeとの文脈が環境をまたいだ瞬間に消えると感じている人

1. 書き出し（あるある共感）
   「あれなんだっけ？」「どこまで進んでた？」「あっちのリポでやったやつ、こっちでも使いたい」
   複数PC・複数リポを使うClaudeユーザーが感じる分断のストレスから入る

2. basic-memory × Google Driveで何が変わったか
   ツールの概要をある程度説明する。前回記事へリンク。「何ができるようになったか」を伝える

3. 運用の流れ（核心）
   - 作業開始時：記憶を呼び起こしてClaudeと共有してから始める
   - 作業終了時：basic-memoryに記憶を保存する
   - この「儀式」が環境・リポジトリをまたいだ連続性を作る

4. 副産物①：「あれなんだっけ」が起きなくなった
   環境をまたいでも・リポジトリをまたいでも前回の続きから始められる（一般化＋最小限の具体例）

5. 副産物②：別の場所で書いた記録が伏線になる
   以前書いたノートが、違う文脈・違うリポジトリで自然と役に立つ（一般化＋最小限の具体例）

6. 副産物③：気づいたら一次情報が溜まっていた
   作ろうとしたわけじゃない。Claudeをさわるほど・試すほど、記事のタネが勝手に溜まる。あとは整形するだけ

7. 締め
   分断に悩んでいる人へ。まずこの仕組みから

---

## 一次情報：読み込んだノートを時系列で並べた結果

**2026-04-11**
- `記憶共有システム記事` 完成・コミット（`44aaed5`）
  - タイトル「2台のPCでClaude Codeの記憶を共有する仕組みを2ヶ月で自作した話」
  - 構成：Phase 0〜5の構築経緯・現在の設計原則
  - 次回予告として「basic-memoryの設定手順を公開予定」と書いた

---

**2026-04-13（Windows → Linux 両環境）**
- Windowsで `dotfiles環境不完全問題の解決` を作業
  - `post-merge` フックを新規作成 → `git pull` で自動的に `setup.sh` が走る仕組みを実装
  - setup.shに「未インストールツール通知」機能追加
  - `jq` をWindowsにインストール（winget）
- Windowsで書いたノート（CRLFで確認）：
  - `問題履歴：WindowsにデフォルトBashがない（フック全停止）`
  - `構造的問題：片側変更・もう片側はpullだけでは気付かない`
  - `作業記録（dotfiles環境不完全問題の解決）`
- Linux側で `改修・動作テスト完了` ノートを書く
- **basic-memory競合問題が発生**
  - Linuxが14回アップロード中にWindowsが書いたノートがDriveから削除される
  - 原因：`rclone sync` が「ローカルにないファイルをDriveから削除する」仕様
  - 修正案（`rclone copy` への変更）はユーザー確認待ちで翌日に持ち越し

---

**2026-04-14（Linux → Windows 両環境）**
- `gdrive-upload.sh` の `rclone sync` → `rclone copy` に修正（commit `4221e8b`）
- `RTK方針確定`
  - Linux：RTKをフックベースで使う
  - Windows：RTKを使わない（デスクトップ版はフック非対応）
  - `rtk-rewrite.sh` にOS判定ロジックを追加（`57def3c`）→ settings.json が Linux/Windows 共通のまま維持できるようになった
  - rtk v0.36.0 をGLIBC不一致でソースビルドで差し替え
  - `git pull前の未コミット確認ルール` ノートを追加
- `v2週次収集の修正・動作確認完了`
- `Discord通知設定・push完了`

---

**2026-04-15**
- ブックマーク精読・調査・提案
- `effortLevel・showThinkingSummaries` 設定

---

**2026-04-16**
- note記事生成（systemd記事）
- systemd × Xブックマーク自動化

---

**2026-04-17**
- X収益化プロジェクト始動
- `INBOX/.gitignore修正`（サブフォルダー対応）

---

**2026-04-18**
- X投稿生成ルール修正・5本書き直し

---

**2026-04-19**
- X投稿実践・セルフ引用検証（@hide390428 で初投稿）

---

**2026-04-21（test2 + dotfiles + test リポジトリ）**
- Xブックマーク週次収集がsystemdタイマーで自動実行（10:45 JST）
  - 18件収集（2026-04-14〜04-20）・GDrive・Discord通知 完了
- `fetch_bookmarks_v2.py` のバグ修正（`card.wrapper` の誤取得問題）
- 週次ブックマーク分析システム構築（x-bookmark-weeklyスキルにステップ5〜8追加）
  - `INBOX/bookmarks_analysis/YYYY-MM-DD/` フォルダー構造整備
  - dotfiles: `8c6ff8f`、test2: `6a9b785`
- `test2リポジトリのGitHubトークンが平文で .git/config に埋め込まれていた問題` 発覚・修正
  - `~/.git-credentials` に移動（600パーミッション）

---

**2026-04-23（testリポジトリ）**
- `notebooklm-py` セットアップ・運用開始
  - ゼロトークンリサーチとして導入（PDFや大資料をNotebookLMに入れてClaudeは質問だけ）
  - 「GPT Image 2 vs NanoBanana Pro 比較調査」ノートブック作成・30ソース追加
  - Claude Codeから直接NotebookLMを操作できることを確認
- `Gemini画像生成システム アップデート`
- `GPT Image 2 API 調査メモ`
- `Jina MCP デフォルト検索設定`（環境変数管理方法確定）

---

**2026-04-24（testリポジトリ）**
- note記事タイトル決定「Claude Codeで検索するたびトークンが飛んでいた。notebooklm-pyで解決した話」
- note記事完成・コミット
