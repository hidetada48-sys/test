# note記事自動生成 作業指示書

ユーザーから「note記事を生成して」と言われたら、以下の手順を順番に実行すること。

---

## 前提

- 作業ディレクトリ: `note_automation/`
- 入力ファイル: `input/` フォルダ内の3つのファイル
- 出力先: `output/` フォルダ（なければ作成する）

---

## 手順

### ステップ1：インプットファイルを読み込む

以下の3ファイルを読み込む：

- `input/01_selling_know_how.txt` → 売れる記事のノウハウ
- `input/02_theme.txt` → 記事テーマ（#で始まる行はコメントなので無視）
- `input/03_my_info.txt` → 筆者の一次情報

### ステップ2：テーマをWeb調査する（Jina AI MCP使用）

Jina AI MCPの検索ツールを使い、テーマに関連する情報を調査する。

- 検索キーワード例：`{テーマ} 方法 コツ 実践`
- 日本語の情報を優先する
- 5件以上の調査結果を収集する
- **調査が失敗した場合は作業を中断し、ユーザーに報告する**

### ステップ3：記事構成を作成する

以下をすべて踏まえて記事構成を考える：

- 売れる記事ノウハウ（必ず全項目反映）
- 筆者の一次情報
- Web調査結果

構成に含める要素：
- タイトル（読者が思わずクリックしたくなるもの）
- 想定読者
- 各セクションの見出しと目的

### ステップ4：記事本文を執筆する

【執筆ルール】
1. 売れる記事ノウハウを一つ残らず反映すること
2. 筆者の一次情報を具体的なエピソードとして盛り込むこと
3. 読者が「自分のことだ」と感じられる書き出しにすること
4. 見出しは `##` を使うこと
5. 文字数は2000〜4000文字
6. note向けの読みやすい文体（短い文・改行多め）
7. 画像を入れるべき場所に以下の形式でタグを挿入すること：
   - `【画像:サムネイル|説明文】` （記事冒頭）
   - `【画像:本文|説明文】` （本文中、必要な箇所ごと）

### ステップ5：画像プロンプトをJSONファイルに書き出す

記事内の `【画像:〜】` タグを分析し、`output/image_prompts.json` に書き出す。

形式：
```json
[
  {
    "type": "thumbnail",
    "description_ja": "どんな画像か日本語で説明",
    "prompt_en": "Imagen 3向け英語プロンプト（写真調・高品質・テキストなし）",
    "aspect_ratio": "16:9"
  },
  {
    "type": "body",
    "description_ja": "どんな画像か日本語で説明",
    "prompt_en": "Imagen 3向け英語プロンプト（写真調・高品質・テキストなし）",
    "aspect_ratio": "1:1"
  }
]
```

### ステップ6：記事ファイルを保存する

`output/article_YYYYMMDD_HHMMSS.md` として保存する。

### ステップ7：画像を生成する

以下のコマンドを実行する：

```bash
python3 note_automation/generate_images.py
```

（Windowsの場合は `python` に読み替える）

### ステップ8：完了報告

以下を報告する：
- 記事ファイルのパス
- 生成された画像の一覧
- note.comへの投稿手順（貼り付け方）

---

## GitHubへのpush手順

ユーザーから「pushして」と言われたら、以下を実行すること。

```bash
# input・outputフォルダの変更・追加を全部ステージング
git add note_automation/input/ note_automation/output/

# コミット（日本語でわかりやすく）
git commit -m "記事と画像を更新"

# GitHubへpush
git push origin main
```

- 新規ファイルも既存ファイルの変更も、両フォルダ内のものはすべて対象になる
- `.env`（APIキー）は`.gitignore`で除外済みなので誤ってpushされる心配はない
