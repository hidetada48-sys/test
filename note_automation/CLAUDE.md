# note記事自動生成 作業指示書

ユーザーから「note記事を生成して」と言われたら、以下の手順を順番に実行すること。

---

## 前提

- 作業ディレクトリ: `note_automation/`
- 永続inputファイル（変わらない）:
  - `input/01_selling_know_how.txt` → 売れる記事のノウハウ
  - `input/04_image_style.txt` → 画像のトンマナ指定
- 素材置き場: `input/materials/`（別リポジトリ等から持ち込んだ素材の一時置き場）
- 記事ごとのinput: `input/YYYY-MM-DD/`（記事化を決めたときにユーザーが作成）
  - `02_theme.txt` → 記事テーマ
  - `03_my_info.txt` → 筆者の一次情報
- 記事ごとのoutput: `output/YYYY-MM-DD/`（記事生成時に自動作成）

---

## 手順

### ステップ0：作業フォルダの特定と my_info ドラフト生成

#### 【作業フォルダの特定】

まず `input/` フォルダを確認し、最新の日付フォルダ（例: `input/2026-03-23/`）を特定する。

- 日付フォルダがない場合は作業を中断し、ユーザーに以下を伝える：
  ```
  input/YYYY-MM-DD/ フォルダがありません。
  記事用のフォルダを作成して 02_theme.txt と 03_my_info.txt を用意してください。
  （input/materials/ に素材がある場合はそちらを参考にしてください）
  ```
- 以降の手順では、特定した日付フォルダを作業inputとして使用する

#### 【my_info ドラフト生成】

過去の会話ログ（JSONLファイル）を読み取り、`03_my_info.txt` のドラフトを作成してユーザーに提示する。

**ログファイルの場所：**
`/home/hidetada48/.claude/projects/-home-hidetada48-test/`

**手順：**
1. 上記フォルダ内の `.jsonl` ファイルの件数を確認する
   - **20件を超える場合は作業を中断し、ユーザーに「ログが〇件あります。全件読むとコンテキストが大きくなりますが続けますか？」と確認すること**
   - 20件以下であれば全件読み込む
2. `input/YYYY-MM-DD/02_theme.txt` のテーマを先に確認する
3. ログの中からテーマに関連するユーザーの発言・体験・気づき・エピソードを抽出する
4. 以下の形式でドラフトを提示し、**承認を得るまで次のステップに進まないこと**：

```
【03_my_info.txt のドラフト確認をお願いします】

## きっかけ
- 〇〇〇〇

## 体験・エピソード
- 〇〇〇〇

## 詰まったこと・工夫したこと
- 〇〇〇〇

## 感想・気づき
- 〇〇〇〇

このドラフトでよければ「OK」、修正があればご指示ください。
OKの場合、このドラフト内容を03_my_infoとして記事生成に使用します。
```

### ステップ0.5：有料記事にするか確認する

ユーザーに以下を聞き、**回答を得るまで次のステップに進まないこと**：

```
この記事は有料部分を入れますか？

- 「有料あり」の場合：ノウハウ全15項目・品質評価全項目を適用します
- 「無料のみ」の場合：項目3（無料部分の設計）・4（有料ラインの設定）・5（セールス文）を無視して進めます
```

回答に応じて以降のステップで適用するノウハウ項目・品質評価項目を切り替える：

| モード | 適用するノウハウ項目 | 品質評価で無視する項目 |
|--------|-------------------|----------------------|
| 有料あり | 全15項目 | なし |
| 無料のみ | 1・2・6〜15（3・4・5を除く） | 有料ライン・セールス文・無料部分設計に関する指摘 |

---

### ステップ1：インプットファイルを読み込む

#### 【画像スタイル参照ファイルのチェック】

まず `input/YYYY-MM-DD/` フォルダ内に `.png` / `.jpg` / `.jpeg` ファイルがあるか確認する。

**画像ファイルが見つかった場合：**

1. 画像を読み込み、以下の観点でスタイルを分析する（被写体・内容・テキストは無視）：
   - 背景色・グラデーション（方向・色値）
   - アクセントカラーとその使われ方
   - レンダリングスタイル（3D CG / フラット / イラスト等）
   - エフェクト（ネオングロー / パーティクル / 被写界深度等）
   - 構図・レイアウト（ゾーン分割 / 奥行き層数等）
   - タイポグラフィの体裁（フォントの重さ・サイズ比・色・配置）
   - 全体の雰囲気・密度感

2. `input/04_image_style.txt` が既に存在する場合は、上書き前にユーザーへ確認を取る：
   ```
   inputフォルダに参照画像（ファイル名）が見つかりました。
   スタイルを分析し、04_image_style.txt を上書き生成します。
   現在の04_image_style.txtは削除されます。よろしいですか？（OK / スキップ）
   ```

3. OKの場合（または04_image_style.txtが存在しない場合）、分析結果を `input/04_image_style.txt` として保存する。
   - **具体的な被写体・オブジェクト（スマートフォン等）は記録しない**
   - 色・エフェクト・レンダリング・構図・タイポグラフィの体裁のみを記録する
   - ステップ5で記事テーマに合うオブジェクトを選ぶ

**画像ファイルがない場合：** 既存の `04_image_style.txt` をそのまま使用する。

#### 【通常インプット読み込み】

以下の4ファイルを読み込む：

- `input/01_selling_know_how.txt` → 売れる記事のノウハウ
- `input/YYYY-MM-DD/02_theme.txt` → 記事テーマ（#で始まる行はコメントなので無視）
- `input/YYYY-MM-DD/03_my_info.txt` → 筆者の一次情報
- `input/04_image_style.txt` → 画像のトンマナ・スタイル指定（上記で生成済みの場合はそちらを使用）

### ステップ2：Web調査の要否を判断し、必要なら実施する

まず以下の基準でWeb調査が必要かどうかを判断し、**ユーザーに宣言してから進むこと**：

**調査不要なケース（スキップしてよい）：**
- `03_my_info.txt` に体験・エピソードが十分に書かれている
- 記事が「開発記録」「体験談」「作ってみた」系で、外部情報を補う必要がない
- Web情報を混ぜると一次情報の純度が下がると判断できる

**調査が有効なケース：**
- テーマの背景知識・統計・トレンドが必要な場合
- 読者が「なぜこれが重要か」を理解するために外部情報が役立つ場合

**宣言の例：**
- 調査する場合：「テーマの背景情報が薄いためWeb調査を行います」
- スキップする場合：「一次情報が充実しているためWeb調査はスキップします」

**調査を実施する場合：**

#### 【SEOキーワード調査（必ず最初に実施）】

Jina AI MCPの検索ツールを使い、以下を調査する。

1. **競合note記事の確認**
   - 検索クエリ：`{テーマ} site:note.com`
   - 上位3〜5件のタイトル・見出し構成を確認する
   - 「書かれていないこと・角度」を把握する

2. **検索需要の確認**
   - 検索クエリ：`{テーマ} やり方`、`{テーマ} できない`、`{テーマ} コツ` など
   - 実際に検索されているキーワードの組み合わせを把握する
   - ロングテールキーワード（複数語の組み合わせ）を候補として記録する

3. 調査結果はステップ3のタイトル・目次設計に必ず反映させる
4. **調査が失敗した場合は作業を中断し、ユーザーに報告する**

#### 【テーマ関連情報の調査】

Jina AI MCPの検索ツールを使い、テーマに関連する背景情報を調査する。

- 検索キーワード例：`{テーマ} 方法 コツ 実践`
- 日本語の情報を優先する
- 5件以上の調査結果を収集する

### ステップ3：記事の目次を提示し、承認を得る

以下をすべて踏まえて記事構成を考える：

- 売れる記事ノウハウ（必ず全項目反映）
- 筆者の一次情報
- Web調査結果

**【SEOキーワードの選定】**
ステップ2のSEOキーワード調査結果をもとに、タイトルと見出しを設計する。

**タイトルのルール：**
- 公式：`「対象」＋「テーマ」＋「得られる結果」`
- メインキーワードをタイトルの**前半**に入れる
- 文字数は**32文字以内**（検索結果で途切れないよう）
- 競合記事にない角度・切り口を選ぶ

**見出し（H2）のルール：**
- 各H2にキーワードまたは関連語を自然に入れる
- 「〇〇とは？」「〇〇の違いは？」など疑問形も検索意図に合いやすい
- 見出し直後の冒頭文にもキーワードを自然に入れる

**本文冒頭のルール：**
- 記事冒頭100文字以内に結論・キーワードを入れる（AI検索にも引用されやすくなる）

ユーザーに以下の形式で目次を提示し、**承認を得るまで次のステップに進まないこと**：

```
【目次の確認をお願いします】

タイトル案：〇〇〇〇
想定読者：〇〇〇〇
想定検索キーワード：〇〇〇〇

## 見出し1
## 見出し2
## 見出し3
...

この目次でよければ「OK」、変更があればご指示ください。
```

### ステップ4：記事本文を執筆する

目次が承認されたら本文を執筆する。画像タグは挿入しない（本文は純粋な文章のみ）。

【執筆ルール】
1. 売れる記事ノウハウを一つ残らず反映すること
2. 筆者の一次情報を具体的なエピソードとして盛り込むこと
3. 読者が「自分のことだ」と感じられる書き出しにすること
4. 見出しは `##` を使うこと
5. 文字数は3000〜5000文字
6. note向けの読みやすい文体（短い文・改行多め）

### ステップ4.5：記事本文の確認を取る

記事本文が完成したら、ユーザーに提示して確認を得る。
**承認を得るまで次のステップ（画像プラン）に進まないこと。**

提示形式：

```
【記事本文の確認をお願いします】

（記事本文をそのまま出力する）

内容・文体・構成に問題なければ「OK」とお知らせください。
修正がある場合はご指示ください。修正後に再提示します。
```

---

### ステップ5：画像の提案を提示し、承認を得る

本文が完成したら、まず `input/04_image_style.txt` を読み込む（このステップの前にまだ読んでいなければ）。その上で本文の構成を踏まえて画像の挿入箇所・枚数・内容を提案する。

**承認を得るまで次のステップに進まないこと。**

【サムネイルには以下を必ず含めること】
- メインコピー：記事タイトルをキャッチーに圧縮した言葉（読者が一瞬で意味をつかめるもの）
- サブコピー：1〜2行で補完する言葉
- 構図の方針：左右の役割（Before/After等）・視線の流れ・主要要素3つ以内

【本文画像には以下を含めること】
- 挿入位置（「## 見出し名」の後）
- 表現する概念・状態（具体的なビジュアルイメージ）

```
【画像プランの確認をお願いします】

合計〇枚を予定しています。

1. サムネイル（記事冒頭）
   - メインコピー：〇〇〇〇
   - サブコピー：〇〇〇〇
   - 構図：左＝〇〇（Before）、右＝〇〇（After）、視線は左→右

2. 本文画像①（「## 見出し名」の後）
   - 表現するもの：〇〇〇〇
   - ビジュアルイメージ：〇〇〇〇

...

枚数や内容を変更したい場合はご指示ください。OKであれば「OK」とお知らせください。
```

### ステップ6：JSONファイルに書き出す

**【最初にやること】** `input/04_image_style.txt` を読み込む（ステップ1でまだ読んでいなければ）。以降のプロンプト生成では、このファイルのフィールド値を直接展開して使うこと。抽象的な言い換えは禁止。

本文への画像タグの挿入は行わない。記事本文は純粋な文章のみとする。

承認済みの画像プランをもとに、`output/YYYY-MM-DD/image_prompts.json` に書き出す。（フォルダがなければ作成する）

---

#### 【言語ルール：厳守】

**全プロンプトは日本語のみで記述する。英語プロンプト一切禁止（固有名詞・ブランド名を除く）。**

救済措置（英語が混入してしまった場合）：
- **推奨**：`Place the Japanese text "〇〇" ([English]) at [position]` の形式で日本語を組み込む
- **代替**：日本語訳プロンプトを追記し、Nano Banana Pro には日本語訳のみ使用する

---

#### 【プロンプト生成ルール：9要素】

各画像のプロンプトは以下の9要素で構成する。

**① 冒頭テキスト表示指示（サムネイルは必須）**

```
画像内に『[メインコピー]』という日本語テキストを左上起点で大きく表示。
```

**② 表示環境の指定（サムネイルは必須）**

```
Xのスマホ画面で読まれることを前提に、メインコピーが一瞬で読めるよう2〜3段に分けて強い視線誘導を作る。
```

**③ メインコピー・サブコピー**

ステップ5で承認されたコピーをそのまま使う。

**④ 構図の指定（具体的に）**

- 左右の役割分担（Before/After等）を明示する
- 主要要素は3つ以内に絞ることを明示する
- 視線の流れを明示する（「左から右へ〇〇の流れで見せる」等）

**⑤ 禁止事項の明示**

```
細かい注釈・多数の要素・均等配置・説明図っぽい表現を避ける。
```

**⑥ 色の指定**

`color_system.brand_colors` の値（google_blue・google_red・google_green・google_yellow）を色コードごとそのままプロンプトに展開する。
`layout_principles.structural_anchor` の値をそのままプロンプトに展開する。
`background_colors` の値をそのまま背景色として展開する。

**⑦ テキスト品質の要件（サムネイルは必須）**

`mobile_x_thumbnail_priority.viewing_context` の値をそのまま展開する。
`mobile_x_thumbnail_priority.success_condition` の値をそのまま展開する。

**⑧ フォント・タイポグラフィ**

`typography.font_family.japanese` の値をそのまま展開する。
`typography_for_thumbnail.main_title_position` の値をそのまま展開する。
`typography_for_thumbnail.main_title_layout` の値をそのまま展開する。
`typography_for_thumbnail.subcopy_rule` の値をそのまま展開する。

**⑨ スタイル・トーン・禁止事項**

`information_density.max_major_elements` の値 → 「主要要素は〇個以内に絞る」として展開する。
`information_density.prohibit` の値をそのまま禁止事項として列挙する。
`cards_and_icons.icon_limit` の値をそのまま展開する。
`layout_principles.avoid` の値をそのまま禁止表現として列挙する。
`layout_principles.recommend` の値をそのまま推奨表現として列挙する。
`quality_bar.target_impression` の値をそのまま目標印象として展開する。
`quality_bar.avoid_impression` の値をそのまま禁止印象として展開する。
`quality_bar.positioning` の値をそのまま展開する。

---

#### 【プロンプトテンプレート（サムネイル）】

以下のプレースホルダーを04_image_style.txtの対応フィールドの値で埋めてプロンプトを生成する。

```
画像内に『[メインコピー]』という日本語テキストを左上起点で大きく表示。[typography_for_thumbnail.main_title_layout の値]。サブコピー「[サブコピー]」をその下に[typography_for_thumbnail.subcopy_rule の値]で配置する。

[mobile_x_thumbnail_priority.viewing_context の値]。[mobile_x_thumbnail_priority.success_condition の値]。

[layout_principles.structural_anchor の値]。背景は[background_colors.surface の値]と[background_colors.background の値]を基調とする。

左側には[Before状態のビジュアルを1つだけ]を置く。右側には[After状態のビジュアルを1つだけ]を置く。主要要素は[information_density.max_major_elements の値]個以内に絞る。[cards_and_icons.icon_limit の値]。

[information_density.prohibit の値]を禁止する。[layout_principles.avoid の値]を避ける。[layout_principles.recommend の値]を実現する。

[typography.font_family.japanese の値]・Boldで[typography_for_thumbnail.main_title_position の値]の階層構造。

[quality_bar.positioning の値]。[quality_bar.target_impression の値]な印象にする。[quality_bar.avoid_impression の値]な表現は禁止する。
```

---

#### 【実行後チェックリスト】

プロンプトをJSONに書き出す前に全項目を確認する：

- [ ] 英語プロンプトが混入していない（固有名詞を除く）
- [ ] 冒頭に日本語テキスト表示の指示が明記されている（サムネイル）
- [ ] 表示環境（スマホ・Xタイムライン）が指定されている（サムネイル）
- [ ] 主要要素が3つ以内に絞られている
- [ ] 禁止事項（均等配置・説明図・多数要素）が明示されている
- [ ] Google Blue を構造軸として使うことが指定されている
- [ ] テキスト可読性の要件が含まれている（サムネイル）
- [ ] 04_image_style.txtの各フィールド値がプロンプトに直接展開されている（抽象的な言い換えになっていないか確認）

---

#### 【JSONフォーマット】

```json
[
  {
    "type": "thumbnail",
    "description_ja": "どんな画像か日本語で説明",
    "main_copy": "メインコピー",
    "sub_copy": "サブコピー",
    "prompt_ja": "9要素を組み込んだ日本語プロンプト",
    "aspect_ratio": "16:9"
  },
  {
    "type": "body",
    "description_ja": "どんな画像か日本語で説明",
    "prompt_ja": "日本語プロンプト（本文画像は冒頭テキスト指示なし・ビジュアル重視）",
    "aspect_ratio": "1:1"
  }
]
```

### ステップ7：記事ファイルを保存する

`output/YYYY-MM-DD/article_YYYYMMDD_HHMMSS.md` として保存する。（フォルダがなければ作成する）

### ステップ7.5：品質評価を実施する

記事を保存したら、`article-reviewer` エージェントを呼び出して品質評価を行う。

```
use_mcp_tool またはサブエージェントとして article-reviewer を呼び出す
対象ファイル: output/YYYY-MM-DD/article_YYYYMMDD_HHMMSS.md
```

**評価結果に応じた対応：**
- **PASS** の場合：ステップ8（画像生成）に進む
- **REVISE** の場合：改善点をもとに記事を修正し、再度 article-reviewer で評価する
  - 修正→再評価のループは最大3回まで
  - 3回後もREVISEの場合はユーザーに判断を仰ぐ

### ステップ8：画像を生成する

以下のコマンドを実行する：

```bash
python3 note_automation/generate_images.py
```

（Windowsの場合は `python` に読み替える）

### ステップ8.5：X投稿を生成する

画像生成完了後、ユーザーへの確認なしに自動でX投稿を生成する。
下記「X投稿生成」セクションの手順をそのまま実行し、`output/YYYY-MM-DD/x_posts.md` に保存する。

### ステップ9：完了報告

以下を報告する：
- 記事ファイルのパス
- 生成された画像の一覧
- x_posts.mdのパス
- note.comへの投稿手順（貼り付け方）

---

## X投稿生成

ユーザーから「X投稿を生成して」と言われた場合、またはステップ8.5として自動実行する場合、以下の手順を実行すること。

### 前提

- 入力：直近の `output/YYYY-MM-DD/article_*.md`（最新の記事ファイル）
- 出力：`output/YYYY-MM-DD/x_posts.md`

### 生成内容

#### ① X長文記事

note記事をそのまま貼らず、「感情・熱量のある記事」に変換する。

**【なぜ変換が必要か】**
noteは「解説記事」として完成している。解説記事をそのままXに貼ってもバズらない。
Xで伸びるのは「感情・熱量」と「対話を生む投稿」だけ。
→ note記事から「事実・数字・体験」を抽出し、「感情が乗った語り」に作り直す。
→ さらに、X内で価値が完結する投稿にする。「続きはnoteで」という発想は捨てる。

**【変換のポイント】**
| 変換箇所 | note記事（変換前） | X記事（変換後） |
|---|---|---|
| 冒頭 | 「本記事では〇〇を解説します」 | 記事の核心となる「感情」で入る（下記ルール参照） |
| 文体 | 解説・説明口調 | 熱量のある語りかけ口調 |
| 文字数 | 3000〜5000字 | 1500〜2000字（核心だけ抽出） |
| 見出し | `##`（Xでは機能しない） | `▼` `■` |
| リンク | 本文中にnoteリンク | 削除（リプライ欄へ移動） |
| 末尾 | なし | **問いかけ（必須）**＋「詳細はリプライ欄👇」 |

【ルール】
- 文字数：1500〜2000字程度
- **X投稿の役割はX上で価値を完結させること。フォロワーとの対話を生む投稿にする**

  **【手順①】note記事の「最も価値あるノウハウ」を選ぶ**
  - 読者が「これ知りたかった」「自分もやってみたい」と思う部分
  - 失敗談・驚きの発見・Before/Afterの落差が大きい部分
  - note記事の核心となるノウハウを判断する

  **【手順②】そのノウハウをX内で完全に出し切る**
  - 「続きはnoteで」という発想をやめる
  - 手順・コツ・具体例をX投稿内で全部書く
  - X投稿を読んだだけで「価値を得た」と感じる状態を作る
  - noteへの誘導は「さらに深く知りたい人向け」として末尾に添える程度

  **【手順③】冒頭は感情で入る（下記2パターンから選ぶ）**

  ① 便利さへの感動（「こんなことができた」系）
    例：「コードは一行も書いていない。なのに毎週勝手に動く。」
        「3つのファイルを用意するだけで記事が完成した。」

  ② 衝撃・想定外（「まさかそうだったのか」系）
    例：「設定した。動いているはず。一度も動いていなかった。」
        「は？2週間、Windowsには届いていなかった。」

  **【手順④】末尾に必ず問いかけを入れる（対話誘発フック）**
  - 「あなたはどうしてますか？」
  - 「同じ経験した人いますか？」
  - 「〇〇で困ったことありますか？」
  - 問いかけの後に「詳細はリプライ欄👇」を添える

- 「先生として教える」ではなく「一緒に熱狂している人」として書く
- 見出し代わりに「▼」「■」を使う
- 短い文・改行多め（Xの読みやすさに合わせる）
- noteリンクは本文に入れない（リプライ欄に置くため）

#### ② ショートポスト（3パターン）

以下の3つの型で、それぞれ140〜280字のショートポストを生成する。
**いずれの型も末尾に問いかけを入れること（リプライを誘発するため）。**

```
型1（共感・あるある型）:
〇〇、最初マジで意味わからなかった。
「なんでこうなってるんだ…」って〇分悩んだ結果、
原因は〇〇だった。
同じ経験した人いる？

型2（ノウハウ完結型）:
〇〇のコツ、3つだけ。

① 〇〇
② 〇〇
③ 〇〇

これだけで〇〇が変わった。
試した人いたら感想聞かせて。

型3（議論・問いかけ型）:
〇〇ってみんなどうしてる？
自分は〇〇という方法でやってるけど、
もっといいやり方があれば知りたい。
```

#### ③ リプライ欄テキスト

```
詳しくはnoteに書きました👇
（noteのURLをここに貼ってください）
```

### 出力フォーマット

`output/YYYY-MM-DD/x_posts.md` に以下の形式で保存する：

````markdown
# X投稿セット：[記事タイトル]

生成日：YYYY-MM-DD

---

## ① X長文記事（そのままXに貼る）

（本文）

---

## ② ショートポスト（3パターンから1つ選ぶ）

### 型1（衝撃発見型）
（本文）

### 型2（Before→After型）
（本文）

### 型3（問いかけ型）
（本文）

---

## ③ リプライ欄テキスト

詳しくはnoteに書きました👇
（noteのURLをここに貼ってください）
````

### 完了報告

保存後、以下を報告する：

```
✅ X投稿を生成しました。

📄 保存先：output/YYYY-MM-DD/x_posts.md

運用手順：
1. ① X長文記事をXに投稿する
2. 投稿直後、リプライ欄に ③ のテキスト＋noteのURLを貼る
3. ② ショートポストは別の日に単独で投稿できます（連投不要）
```

---

## GitHubへのpush手順

ユーザーから「pushして」と言われたら、以下を実行すること。

```bash
# input・outputフォルダの変更・追加を全部ステージング
git add note_automation/input/ note_automation/output/

# コミット（日本語でわかりやすく）
git commit -m "記事と画像を更新"

# GitHubへpush
git push origin main
```

- 新規ファイルも既存ファイルの変更も、両フォルダ内のものはすべて対象になる
- `.env`（APIキー）は`.gitignore`で除外済みなので誤ってpushされる心配はない
